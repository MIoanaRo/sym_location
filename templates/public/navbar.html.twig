<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('accueil_home') }}">SymLocation</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#"> <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Nos destinations
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('accueil_home') }}">Liste des conducteurs</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('accueil_home') }}">Liste des vehicules</a>
                    <a class="dropdown-item" href="{{ path('accueil_home') }}">Liste des locations</a>
                </div>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('user_show', { id : app.user.id }) }}">Mon compte : {{ app.user.username }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
            {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_register') }}">Creer un  compte</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">Me conecter</a>
            </li>
            {% endif %}

        </ul>
    </div>
</nav>
