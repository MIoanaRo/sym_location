{% extends 'base.html.twig' %}

{% block title %}Conducteur{% endblock %}

{% block body %}
    <h1>Conducteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conducteur.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ conducteur.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ conducteur.lastname }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ conducteur.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conducteur_index') }}">back to list</a>

    <a href="{{ path('conducteur_edit', {'id': conducteur.id}) }}">edit</a>

    {{ include('conducteur/_delete_form.html.twig') }}
{% endblock %}
